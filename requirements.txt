# Install everything in one shot:
#   pip install -r requirements.txt
#
# Or per subfolder (only what you need):
#   pip install -r api/requirements.txt
#   pip install -r signal-processing/manual-filtering/requirements.txt
#   pip install -r signal-processing/model-finetuning/requirements.txt

# ── Acquisition and hardware (api/, control/) ─────────────────────────────────
brainflow>=5.12
pyautogui>=0.9.54

# ── Visualization (api/) ──────────────────────────────────────────────────────
matplotlib>=3.8
PyQt6>=6.6
pyqtgraph>=0.13

# ── Signal processing (signal-processing/manual-filtering/) ──────────────────
mne>=1.7
scipy>=1.12

# ── EEGNet model (signal-processing/model-finetuning/) ───────────────────────
braindecode[hub]>=0.8
torch>=2.2

# ── Common ────────────────────────────────────────────────────────────────────
numpy>=1.26
scikit-learn>=1.4
