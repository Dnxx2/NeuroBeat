# Instala todo el repo de una sola vez:
#   pip install -r requirements.txt
#
# Alternativa por subcarpeta (solo lo que necesitas):
#   pip install -r api/requirements.txt
#   pip install -r signal-processing/manual-filtering/requirements.txt
#   pip install -r signal-processing/model-finetuning/requirements.txt

# ── Adquisición y hardware (api/, control/) ───────────────────────────────────
brainflow>=5.12
pyautogui>=0.9.54

# ── Visualización (api/) ──────────────────────────────────────────────────────
matplotlib>=3.8
PyQt6>=6.6
pyqtgraph>=0.13

# ── Procesamiento de señal (signal-processing/manual-filtering/) ──────────────
mne>=1.7
scipy>=1.12

# ── Modelo EEGNet (signal-processing/model-finetuning/) ──────────────────────
braindecode[hub]>=0.8
torch>=2.2

# ── Común a todo ──────────────────────────────────────────────────────────────
numpy>=1.26
scikit-learn>=1.4
